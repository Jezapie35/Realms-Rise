import React from 'react';
import AssetIcon from '../AssetIcon';

export default function AscensionIcon({ size = 48 }: { size?: number }) {
  return <AssetIcon icon="ascension_robe" size={size} />;
}
