import React from 'react';
import AssetIcon from '../AssetIcon';

export default function CelestialSpire({ size = 72 }: { size?: number }) {
  return <AssetIcon icon="celestial_spire" size={size} />;
}
