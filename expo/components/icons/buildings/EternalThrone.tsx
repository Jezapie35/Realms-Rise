import React from 'react';
import AssetIcon from '../AssetIcon';

export default function EternalThrone({ size = 72 }: { size?: number }) {
  return <AssetIcon icon="eternal_throne" size={size} />;
}
